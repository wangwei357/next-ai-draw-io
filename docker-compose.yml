services:
  drawio:
    image: jgraph/drawio:latest
    ports: ["8400:8080"]
  next-ai-draw-io:
    build:
      context: .
      args:
        - NEXT_PUBLIC_DRAWIO_BASE_URL=http://10.246.107.247:8400
        # Uncomment below for subdirectory deployment
        # - NEXT_PUBLIC_BASE_PATH=/nextaidrawio
    ports: ["8500:3000"]
    env_file: envenvfile
    environment:
      # For subdirectory deployment, uncomment and set your path:
      # NEXT_PUBLIC_BASE_PATH: /nextaidrawio
    depends_on: [drawio]
